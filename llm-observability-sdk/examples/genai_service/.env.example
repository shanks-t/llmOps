# Google API Key (required)
GOOGLE_API_KEY=your-google-api-key

# Service identification
SERVICE_NAME=fastapi-genai-service
DEPLOYMENT_ENV=local

# OTLP exporter (Jaeger/Tempo)
OTEL_ENABLED=true
OTEL_ENDPOINT=http://localhost:4318/v1/traces

# Phoenix/Arize AX exporter (GenAI traces only)
# Mode: "phoenix" (open source), "ax" (enterprise), or "disabled"
ARIZE_MODE=phoenix
ARIZE_ENDPOINT=http://localhost:6006/v1/traces

# For Arize AX (enterprise) only - uncomment and configure:
# ARIZE_MODE=ax
# ARIZE_ENDPOINT=https://your-ax-cluster.example.com/v1
# ARIZE_API_KEY=your-api-key
# ARIZE_SPACE_ID=your-space-id

# Debug: print spans to console
OTEL_CONSOLE_DEBUG=false

# GCS configuration for medical records pipeline
GCS_PROJECT_ID=llmops-genai
GCS_BUCKET_NAME=patient-records-genai
GCS_RAW_PREFIX=raw
GCS_SUMMARIES_PREFIX=summaries
GCS_INDEX_FILE=index.json


just run
uv run uvicorn main:app --reload
INFO:     Will watch for changes in these directories: ['/Users/treyshanks/workspace/llmOps/llm-observability-sdk/examples/genai_service']
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [29914] using WatchFiles
INFO:     Started server process [29916]
INFO:     Waiting for application startup.
[observability] OTLP exporter enabled → http://localhost:4318/v1/traces
[observability] Arize AX exporter enabled via llmops SDK (GenAI spans only)
[observability] Initialized: service=fastapi-genai-service, env=local, otel=True, arize_mode=ax
INFO:     Application startup complete.
INFO:     127.0.0.1:53402 - "POST /chat HTTP/1.1" 200 OK
Transient error Internal Server Error encountered while exporting span batch, retrying in 1.06s.
Transient error Internal Server Error encountered while exporting span batch, retrying in 2.13s.
Transient error Internal Server Error encountered while exporting span batch, retrying in 4.21s.
Failed to export span batch code: 500, reason:�All spans failed. Found 3 errors: rpc error: code = InvalidArgument desc = model_id span resource attribute or arize.project.name span attribute is required; rpc error: code = InvalidArgument desc = model_id span resource attribute or arize.project.name span attribute is required; rpc error: code = InvalidArgument desc = model_id span resource attribute or arize.project.name span attribute is required